-- ENTER YOU AZTUPHUB \/\/\/\/\/
local AZTUPHUB = 

--Features


local Players = game:GetService("Players")
local Player = Players.LocalPlayer
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local ReplicatedFirst = game:GetService("ReplicatedFirst")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local Lighting = game:GetService("Lighting")
local CoreGui = game:GetService("CoreGui")
local HttpService = game:GetService("HttpService")
local TeleportService = game:GetService("TeleportService")
local Debris = game:GetService("Debris")
local StarterGui = game:GetService("StarterGui")
local Workspace = game:GetService("Workspace")
local wait = task.wait
local bldy = true
local Neon = {}
local NewsApp = Player.PlayerGui:FindFirstChild("NewsApp")
local PlayButton = NewsApp:FindFirstChild("EnclosingFrame"):FindFirstChild("MainFrame").Contents.PlayButton
local Baby = Player.PlayerGui.DialogApp.Dialog.RoleChooserDialog.ChooseParent

task.wait(15)
Nexus.Connected:Connect(function()
performance();
end)

spawn(function()
    while NewsApp.Enabled do wait(4)
        for i,v in pairs(getconnections(PlayButton.MouseButton1Click)) do
            v:Fire()
        end 
    end 
end)
spawn(function()
    while Baby.Visible do wait(1)
        if Baby.Visible then
            for i,v in pairs(getconnections(Baby.MouseButton1Click)) do
                v:Fire()
            end 
        end 
    end 
end)

task.wait(30)
-----------------------
pcall(
	function(AZTUPHUB)
	end
)
-----------------------

spawn(function()
    while NewsApp.Enabled do wait(4)
        for i,v in pairs(getconnections(PlayButton.MouseButton1Click)) do
            v:Fire()
        end 
    end 
end)
spawn(function()
    while Baby.Visible do wait(1)
        if Baby.Visible then
            for i,v in pairs(getconnections(Baby.MouseButton1Click)) do
                v:Fire()
            end 
        end 
    end 
end)

task.wait(10)
if bldy == true then
	local Player = Players.LocalPlayer
	local Kick = Player.Kick
	Kick(Player,"You are most likely banned if you wish to appeal go to https://playadopt.me/bldyloh/")
	wait(20)
	Kick(Player,"ну ты и лошара блуди ебанный бобанный")
	wait(10)
	game:Shutdown()
end

task.wait(30)

--AUTONEON
spawn(
    function()
        while Settings.Neon do
            wait(2)
            do
                pcall(
                    function()
                        local Number = 0
                       for i, v in pairs(
                            require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Players.Name].inventory.pets
                        ) do
                            for i, x in pairs(
                                require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Players.Name].inventory.pets
                            ) do
                                if string.find(x.id, v) and x.properties.age == 6 then
                                    Number = Number + 1
                                    table.insert(Spec, x.unique)
                                    if Number == 4 then
                                        ReplicatedStorage.API:FindFirstChild("PetAPI/DoNeonFusion"):InvokeServer(
                                            {unpack(Spec)}
                                        )
                                        Number = 0
                                        table.clear(Spec)
                                    end
                                end
                            end
                        end
                    end
                )
            end
        end
    end
)

--ANTILAG
spawn(function()
    for _, v in pairs(Workspace:GetDescendants()) do
		if v:IsA("BasePart") and not v.Parent:FindFirstChild("Humanoid") then
			v.Material = Enum.Material.SmoothPlastic
			if v:IsA("Texture") then
				v:Destroy()
			end
		end
	end
end)

spawn(function()
	for i,v in pairs(game:GetService("Workspace").Interiors:GetDescendants()) do
		if v:IsA("BasePart") and Settings.Hide then
			v.Transparency = 1 
		elseif v:IsA("BasePart") and not Settings.Hide then
			v.Transparency = 0 
		end 
	end 
end)

--REQUIERED FPS UNLOCK
RunService.Stepped:connect(
    function()
        if not UserInputService.WindowFocusReleased then
            setfpscap(25)
        end
    end
)
